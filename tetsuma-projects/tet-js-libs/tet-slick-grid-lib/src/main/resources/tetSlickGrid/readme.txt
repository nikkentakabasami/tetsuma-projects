http://localhost:8080/enettc-web/v2/dashboard_user_request_last_registered.jsp

---------
примеры


--------



todo

локальная фильтрация
  не делать - если фильтра не задан!!!  filterRows(sourceData, filter){


вывести фильтрацию локальных данных  отдельную функцию

снизить связность - через события

заголовок таблицы сносит при задании столбцов

Панель загрузки!!!
showWaitPanel("asdf")


изм размер столбцов - не перерисовывать полностью

date dialog, number dialog
multiselect



уничтожение

страницы с примерами, хелп

restoreColsWidth - сохранение ширины столбцов, сортировки и прочего  
	$('input.number-input').dblclick(function() {
	$('input.date-input').each(function(index) {
		initTableDateRange($(this));
	});



saveColsWidth();

remove field from column

после скрытия столбцов - ширина заголовка не меняется
