openlayers


vectorLayer.getFeatures(pixel)
map.hasFeatureAtPixel(e.pixel);

javascript
  Functions
  canvas

-------------


const windData = new Promise((resolve, reject) => {
  const image = new Image();
  image.onload = () => {
    const canvas = document.createElement('canvas');
    const width = image.width;
    const height = image.height;
    canvas.width = width;
    canvas.height = height;

    const context = canvas.getContext('2d');
    context.drawImage(image, 0, 0);
    const data = context.getImageData(0, 0, width, height).data;
    resolve({data, width, height});
  };
  image.onerror = () => {
    reject(new Error('failed to load'));
  };
  image.src = './data/wind.png';
});


---------------------



/home/tetsuma/work/tmp/ol/


выбранная демка - задавать через параметр запроса.

изучить по js:

ReadableStream
URL - blob

--------------
скрытый элемент:
      a.skiplink {
        position: absolute;
        clip: rect(1px, 1px, 1px, 1px);
        padding: 0;
        border: 0;
        height: 1px;
        width: 1px;
        overflow: hidden;
      }

gifler lib
https://openlayers.org/en/latest/examples/animated-gif.html      
      